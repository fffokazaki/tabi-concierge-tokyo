/**
 * 答えられなかった問いが「記録され、都へのデータ公開リクエストへ還元される」ことの注記（Issue #192）。
 *
 * エスカレーションの仕組み自体は前からサーバー側で動いている（`worker/core/gaps.ts` が
 * 未回答の発生時点で D1 `gaps` へ書く）。**画面にその説明が1文も無かった**ため、
 * 利用者から見ると「答えられません」で行き止まりに見えていた。DOMAIN.md §7 の
 * 「未回答 → データ公開リクエスト」は企画の芯の半分なので、そこを画面に出す。
 *
 * ## ボタンにしない（2026-08-17 の決定を維持する）
 *
 * 承認済みデザインカンプにあった「この情報をリクエストする」ボタンは、対応するサーバー側の
 * オプトイン機構が無いため意図的に外してある（`DataGapCard` の doc）。記録は自動なので、
 * ボタンは「押さないと申請されない」という**実在しない仕組み**を示唆する。本コンポーネントは
 * その決定を覆すものではない —— 押す操作は増やさず、既に動いている仕組みを述べるだけ。
 *
 * ## 「記録されます」と「変えていきます」で時制を分ける（勝手に整えないこと）
 *
 * - **記録されます**: 実装済みの事実。D1 `gaps` への書き込みは本番で稼働している
 * - **変えていきます**: 意図。都への提出プロセスは**構想**（submission-deck.md の
 *   「設計済み・未実装」）。「提出しました」「送信しました」と書くと嘘になる
 *
 * この切り分けは `docs/first-stage-presentation.md` の発話原稿と同じ。資料と画面で
 * 主張がずれないよう、文言はそちらから借りている。
 *
 * ## 個別の受領証にはできない
 *
 * `d1GapRecorder` は D1 への書き込み失敗を `console.error` に出して**応答は通す**
 * （未回答そのものは正常な応答なので、記録の失敗で 500 にすると「答えられない」という
 * 事実まで利用者に届かなくなる、という意図的な設計）。つまり**応答に記録の成否が載っていない**
 * ため、フロントは「この問いは記録済みです」と個別に断定できない。だからこの文は
 * 仕組みの説明であって、目の前の1件についての受領証ではない。
 * 受領証にするには応答へ成否を載せる必要がある（Issue #194）。
 *
 * `role` は付けない。同じ画面の `DataGapCard` が `role="status"` で読み上げを担っており、
 * 常設の仕組み説明をもう一度ライブリージョンとして読ませる理由がない。
 *
 * ## `reason` で出し分けない（一度そう実装しかけて、実測で誤りだと分かった）
 *
 * 「フロント側が組み立てた `reason: "other"`（`buildPlan.ts` / `buildRecommendations.ts` の
 * 「候補のデータセットから…取り出せませんでした」）にはサーバーの記録が無いから、`other` の
 * ときは黙るべきだ」という判定を検討したが、**取らない**:
 *
 * - **サーバーが返す `other` も記録されている。** 渋谷×ショッピング／ナイトライフは応答全体が
 *   `unanswered` / `other` で返り（CLAUDE.md の実測記録）、`gaps` テーブルにも入る。
 *   `other` で黙ると、**記録されている実在の経路で注記が消える**
 * - **フロント側の `other` は必ず原因の `gaps` を伴う。** あの分岐は候補ごとの
 *   `aggregate_dataset` が全件未回答だったときに入るので、その未回答（サーバーが記録済み）が
 *   そのまま `gaps` に載る。記録が1件も無いのは「`answered` なのに候補0件」という異常応答の
 *   場合だけで、それは未公開データの話ではなく応答の不整合
 *
 * `reason` は応答全体を代表する1つでしかなく（Issue #94。個々の理由は `gaps` が運ぶ）、
 * 記録の有無を判別する材料には使えない。**この文が機構の説明であって目の前の1件の受領証では
 * ない**ことが、出し分けの代わりの担保になっている。
 */
export function GapEscalationNote() {
  return (
    <p className="gap-escalation-note">
      <svg
        className="gap-escalation-note__icon"
        viewBox="0 0 12 12"
        fill="none"
        stroke="currentColor"
        strokeWidth="1.2"
        strokeLinecap="round"
        strokeLinejoin="round"
        aria-hidden="true"
      >
        <path d="M6 1.5v5" />
        <path d="M3.75 4.25 6 6.5l2.25-2.25" />
        <path d="M2 9.75h8" />
      </svg>
      答えられなかった問いは記録されます。何が足りないのかを、東京都へのデータ公開リクエストに変えていきます。
    </p>
  );
}
