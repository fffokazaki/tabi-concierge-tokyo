---
title: "submission-deck"
version: "1.21.0"
status: "draft"
owner: "@fffokazaki"
created: "2026-08-21"
updated: "2026-08-23"
changeImpact: "medium"
---

<!-- markdownlint-disable MD013 MD024 MD025 -->

# 提出資料｜16:9・14枚構成

- 提出締切: **2026-08-23（日）17:00**（<https://form.jotform.com/261870604352051>）
- 形式: PowerPoint または PDF・16:9 横
- 添付: 画面キャプチャ **1600×900px** を1〜3点
- 登録: 利用オープンデータ 最低1件〜最大10件（データURL＋タイトル）
- 必須4項目: ①課題及び解決策 ②プロダクト ③利用オープンデータ ④チーム紹介

> **この原稿から PPTX を生成するビルダーは [`deck/`](../deck/) にある**（`cd deck && npm run build`）。本書を直したら `deck/build.js` 側も同期すること。`build.js` は冒頭コメントで参照している本書のバージョンを持っており、そこがズレていたら未同期のサインである。
>
> **First Stage の2分スライドとは別のデッキ**（[ROADMAP.md](07-project-management/ROADMAP.md) §4）。本書がフルバージョンで、[first-stage-presentation.md](first-stage-presentation.md) の8枚はここからの切り出し。スライド番号を書くときはどちらのデッキか明示すること。
>
> スライドの実体（PPTX / PDF）と動画は本リポジトリの管理外。本書は構成と文言の SSOT。

## 1. 設計方針

### 「答えられない」を物語の山に置く

出典が付くことを前半で見せてから、**付けられないときは答えない**（スライド8）に落とす。他チームが再現できない差別化はここにしかない。実装済みの `insufficient_granularity` 判定を実画面で見せる。

### 実装済みと構想を視覚的に区別する

このプロダクトは「根拠がなければ答えない」を掲げている。資料自身が根拠のない主張をすれば、その時点で主張が崩れる（[CLAUDE.md](../CLAUDE.md) 絶対ルール #1・#2）。全スライドで次の3層を見分けられる形にし、凡例をスライド4に置く。

| 層 | 定義 | 該当 |
| --- | --- | --- |
| **稼働中** | 本番URLで今動く | 旅のプロフィール入力・プラン生成・あなたへ・出典チップ・`gaps` 記録・D1（10データセット / 1,645スポット）・**`/mcp` のコア3操作公開**（[Issue #117](https://github.com/fffokazaki/tabi-concierge-tokyo/issues/117)）・**Text-to-SQL による D1 実照会**（[Issue #119](https://github.com/fffokazaki/tabi-concierge-tokyo/issues/119)）・**メタデータRAG による自然文の分解**（[Issue #120](https://github.com/fffokazaki/tabi-concierge-tokyo/issues/120)） |
| **設計済み・未実装** | 仕様が文書で確定していて、コードがまだ無い | データ公開リクエストの都への提出・コンシェルジュの別リポジトリ切り出しと MIT での OSS 公開（[Issue #129](https://github.com/fffokazaki/tabi-concierge-tokyo/issues/129)） |
| **デザイン構想** | 画面デザインのみ存在 | スキャン・周辺（`public/showcase/`） |

### 使う数字は3つに絞る

「9,600 ／ 1日200PV ／ 30分→3分」（[CONSTRAINTS.md](01-context/CONSTRAINTS.md) §7）。うち「30分→3分」は、**30分＝開発メンバーが初めてカタログを人手で探した際の体験に基づく目安、3分＝本アプリの操作一巡（条件入力〜出典付きルート表示）の目安**として提示する（生成応答は本番で押下から約13秒後に表示済みを確認・2026-08-22・1回計測。操作一巡の通し計測は行っていない）。統制された比較計測ではないため「実測30分」等の断定表記はしない（[PROJECT.md](01-context/PROJECT.md) §8・[Issue #130](https://github.com/fffokazaki/tabi-concierge-tokyo/issues/130)）。

## 2. スライド構成（14枚）

全スライドの上部ラベルは、応募要件の丸数字ではなく **`現在のページ / 総ページ数　内容名`** で統一する。例：`07 / 14　プロダクト｜訪日外国人向け`。これにより、審査員が資料内の現在位置と各ページの役割を同時に把握できるようにする。

| # | 必須項目 | 内容 | 素材・注記 |
| --- | --- | --- | --- |
| 1 | — | 表紙「9,600のオープンデータを、旅の相棒に。」／**訪日外国人向けAI旅行ガイド**「旅コンシェルジュTOKYO」／**Powered by オープンデータ・コンシェルジュ**／チームshiwata | 旅行アプリがオープンデータ基盤の上で動く2層構造を表紙から示す |
| 2 | ① | 課題A：9,600データセットが公開されている一方、カタログの閲覧は1日約200PV。原因は「1データ＝1アプリ」という活用の型 | — |
| 3 | ① | 課題B：**訪日外国人**は日本の文化・マナーに迷っても、公共データに到達する手段を持たない | — |
| 4 | ① | 解決策：会話から9,600件に到達する入口をつくる。**根拠がなければ答えない**を設計の中心に置く | **3層の凡例をここに置く** |
| 5 | ② | **サービス全体像**：旅の準備から現地まで、AIエージェントが5つの画面で伴走する。旅のプロフィール・プラン・あなたへは実データ接続で稼働。スキャン・周辺はデザイン構想 | 稼働中3画面は現行実装のキャプチャ。スキャン・周辺は `public/showcase/Japanese version.dc.html` から切り出し、各画面に層バッジを付ける |
| 6 | ② | 動く画面①：旅のプロフィール → プラン生成。全提案に出典チップが付く | **キャプチャ候補1** |
| 7 | ② | 動く画面②（訪日外国人向け）：**日本のマナーまで、旅先で案内。** 興味に合う観光地を出典つきで薦め、参拝・銭湯・飲食店など日本独自の作法を同じ画面で確認できる。観光情報の「出典」と、JNTO 等によるマナーの「参考」は明確に分ける | **キャプチャ候補2** |
| 8 | ② | **答えられないものを、オープンデータの要望へ**：答えられないときは、答えません。ラーメン → 粒度不足と判定し理由を返す。未回答の記録・分類は稼働中、東京都への公開リクエスト案への変換は設計済み・未実装 | **キャプチャ候補3**。文言は §3 で固定 |
| 9 | ② | **AIでつくり、AIで届ける。** 上段「つくるAI」は、`docs/`（SSOT）→ Claude Code / Codex → 実装 → 検証の **AI仕様駆動開発**。下段「届けるAI」は、**訪日外国人の自然文の質問** → メタデータRAG → Text-to-SQLによるD1実照会 → 出典つき旅程・マナー。検索・取得・出典強制は稼働中（[Issue #117](https://github.com/fffokazaki/tabi-concierge-tokyo/issues/117) / [#119](https://github.com/fffokazaki/tabi-concierge-tokyo/issues/119) / [#120](https://github.com/fffokazaki/tabi-concierge-tokyo/issues/120)）。自然文をLLMで構造化する既存経路を活かして**多言語対応予定**。マナーは JNTO 等の「参考」としてオープンデータの「出典」から区別する。同じコア3操作を `/mcp` でも公開 | 上下2レーン。稼働中／対応予定／参考をバッジとラベルで区別。詳細は §4 |
| 10 | ② | **2つのAIエージェントが、旅とデータ改善をつなぐ。** 旅コンシェルジュが出典つきで旅行を支援し、答えられなかった問いをオープンデータ・コンシェルジュが `gaps` に記録・分類。東京都への公開リクエスト案の自動生成・提出へつなげる | 記録・分類は稼働中。要望案の自動生成・提出は設計済み・未実装として区別。詳細は §4 |
| 11 | ② | 基盤の開放：コンシェルジュは MCP サーバーとして開放予定。**別リポジトリへ切り出し MIT ライセンスで OSS 公開予定**（[Issue #129](https://github.com/fffokazaki/tabi-concierge-tokyo/issues/129)）。旅行アプリは「最初のクライアント」にすぎない | **OSS 化の対象はコンシェルジュのみ**（旅行アプリ `src/` は対象外）。データ側 CC BY 4.0 とコード側 MIT を**別物として並記** |
| 12 | ③ | 利用オープンデータ10件：タイトル・提供元・役割・ライセンス・カタログURL | [DATABASE.md](02-design/DATABASE.md) §2 の確定表をそのまま |
| 13 | ① | インパクト／KPI：探索30分→3分（体験・操作一巡に基づく目安）、観光の分散、都政への還元 | 「30分は初めてカタログを人手で探した開発メンバーの体験に基づく目安、3分はアプリ操作一巡の目安」と注記（「実測」とは書かない。§1・[Issue #130](https://github.com/fffokazaki/tabi-concierge-tokyo/issues/130)） |
| 14 | ④ | チーム紹介（チームshiwata・3名）。3名を同じサイズの円形ポートレートで表示 | §5。AI仕様駆動開発はスライド9へ集約 |

## 3. スライド8の文言（固定）

このスライドは「主張しすぎない」ことが価値の源泉なので、言い回しを先に確定させる。

- **見出し**: 答えられないときは、答えません
- **上部ラベル**: `08 / 14　プロダクト｜答えられない場合`（§2 の統一規則。v1.16.0 で定めた「プロダクト ②　答えられないものを、オープンデータの要望へ」は、**丸数字を外した v1.20.0 でラベルから消えている**）
- **「オープンデータの要望へ」の置き場所**: ラベルから消えた分、この訴求は**本文最下段の1行が担う**（「現在はここまで稼働中。将来は『このデータをオープンデータとして用意してほしい』という東京都への要望案へ整えます。」）。冗長に見えても消さないこと —— 消すとスライド8から未回答→データ改善の筋が失われる
- **キャプション**: このケースでは **粒度不足** と判定し、理由とともに返します
- **実測の並置**: 東京都内の飲食店バリアフリー情報 210件中、ラーメン店は3件（板橋区・武蔵野市・青梅市）。代表エリアには0件
- **状態の区別**: 未回答の記録・分類は稼働中。将来は「このデータをオープンデータとして用意してほしい」という東京都への公開リクエスト案へ整える（設計済み・未実装）

### 使ってはいけない表現

- 「データが無いものには答えません」のような**一般化した断定**
- 「〇〇という切り口で検証済み」という**広い主張**

検証済みなのは、このラーメンの粒度不足の実測（[DATABASE.md](02-design/DATABASE.md)「既知のデータ欠損」）と、渋谷のカタログに観光・文化施設データが1件も無いことだけ。それ以上を主張すると、この資料が回避しようとしている過剰主張を資料自身がやることになる。

## 4. AI技術スライド（スライド9）

### 見出し

- **上部ラベル**: `09 / 14　仕組み｜AI × OPEN DATA`（§2 の統一規則。丸数字は v1.20.0 で削除済み）
- **タイトル**: AIでつくり、AIで届ける。

### 上段「つくるAI」

`人＋AIで仕様を考える → docs/（SSOT） → AIが実装・検証 → 人が動作確認`

- **ラベル**: AI仕様駆動開発
- **状態**: 稼働中
- **人の役割**: AIと一緒に仕様を考えて意図を決め、最後に実際の動きを確認する
- **AIの役割**: 仕様を基に Claude Code / Codex が実装し、テストなどの機械検証まで進める

### 下段「届けるAI」

`訪日外国人の自然文の質問 → メタデータRAG → Text-to-SQL / D1実照会 → 出典つき旅程・マナー`

- **稼働中**: メタデータRAG、Text-to-SQL、D1実照会、出典強制
- **対応予定**: 自然文をLLMで構造化する既存経路を活かした多言語対応。実装・動作検証は未実施のため「簡単」「すぐできる」とは書かない
- **参考**: マナーは JNTO 等の外部情報を「参考」として表示し、東京都オープンデータの「出典」と見た目・ラベルを分ける（[ADR-012](06-reference/DECISIONS.md)）
- **経路の限定**: 興味チップだけの構造化入力はメタデータRAGを飛ばして Text-to-SQL へ進む。全入力が4段を直列に通るようには描かず、スライド上の注記と発表者ノートで限定する（[API.md](02-design/API.md) §3.1）
- **基盤の開放**: 同じコア3操作を `/mcp` でも公開中

### 2つのAIエージェント（スライド10）

- **旅コンシェルジュ AIエージェント（稼働中）**: 訪日外国人の質問を受け、出典つきの旅程・レコメンド・マナー参考情報を返す。根拠を付けられない問いは答えを作らず未回答として返す
- **オープンデータ・コンシェルジュ AIエージェント**: 未回答を `gaps` に記録し、データ未公開・粒度不足・対象エリア外などに分類して束ねるところまでは稼働中。何が不足しているかを東京都への公開リクエスト案へ整える自動生成・提出は設計済み・未実装
- **目指す姿**: 旅のサポートから、足りないオープンデータの改善までを、2つのAIエージェントが一つの循環として動かす

## 5. チーム紹介（スライド14）

チームshiwata・3名。役割分担を載せる。AI仕様駆動開発はスライド9へ集約し、ここでは重複させない。

| 氏名 | 役割 |
| --- | --- |
| shiwata | 発起人／プレゼンター／証券会社勤務のアラフィフ |
| sho gamoh | 本サービスの発案者／フロントエンド担当／18-year-old creator |
| フトシ | オープンデータ・コンシェルジュ発案者／バックエンド・クラウド・AI駆動開発支援 |

3名とも本人提供の画像を使用し、同じサイズの円形ポートレートとして表示する。shiwataの横長イラストとsho gamohの写真は、imagegenで顔〜肩が中心のイラスト調アバターへ加工する。

## 6. 提出前チェックリスト

- [x] **資料に埋め込むキャプチャ3枚（スライド6/7/8）を撮り直した**（2026-08-22・現本番URL `tokyo-odh-091`・**480×874**）。これは資料内に貼る端末フレーム画像で、**下の提出フォーム添付（1600×900px）とは別の成果物**。当初はスライド6のみの想定だったが、**7/8 も変わっていた** ―― [Issue #192](https://github.com/fffokazaki/tabi-concierge-tokyo/issues/192) が「答えられなかった問いは記録されます。…」の注記を未回答表示の脇に追加しており、旧キャプチャにはこれが写っていない。操作条件は従来どおり、興味チップのみ・自由文は空、スライド6はラーメンを外した「文化・家族向け・自然」、スライド7は文化チップ、スライド8はラーメンチップ
- [x] **スライド6/7のキャプチャは画面上部のみ**。フレーム全体（`.app-shell`）の高さは実測で **プラン 480×1113 / あなたへ 480×1550**（2026-08-22）。全高を貼ると資料の枠（`screenSlide` の `w: 2.86, h: 5.21`・比率 0.549）で幅 **1.6〜2.3in** の短冊になり、本文が **2.8pt 相当**まで潰れるため、**比率を保って上部を表示する**方を採った。

  | 画像 | 実寸 | 全高で貼った場合の幅 | 本文の実効サイズ |
  | --- | --- | --- | --- |
  | プラン | 480×1113 | 5.21 × 480/1113 = 2.25in | 3.9pt |
  | あなたへ | 480×1550 | 5.21 × 480/1550 = 1.61in | **2.8pt** |
  | ラーメン | 480×874 | 5.21 × 480/874 = 2.86in（枠と一致） | 4.9pt |

  pt は **アプリ本文の 11.5px**（本番で実測した最頻の本文サイズ）を基準に `11.5 / 480 × 幅in × 72` で換算した値。**旧キャプチャは3枚とも 480×874 だが、当時のフレーム高を測った記録が無い**ため、当時 `.app-shell` が 874 に収まっていたのか、当時も上部だけを写していたのかは確定できない（#188 のイラストと #192 の注記でいまが伸びていることは実測済み）。タブバーが写らなくなったので、6/7 のキャプションからタブの断り書きを外した（`SCROLL_CAPTION`）。スライド8は 480×874 に収まりタブバーまで写るため従来の `TAB_CAPTION` のまま
- [x] **提出フォームに添付する画面キャプチャ 1600×900px を3点用意した**（2026-08-22・本番 `tokyo-odh-091`・興味チップのみ・自由文は空）。**資料に埋め込む 480×874 の端末フレーム画像とは別物**で、そちらを添付しても規定を満たさない（[CONSTRAINTS.md](01-context/CONSTRAINTS.md) §7 の変更不可の規定）。**`deck/submit/` に追跡している**（撮り直すと `/gaps` の件数が変わって同じものにならないため、生成物ではなく提出物として固定する）。`~/Documents/tabi-concierge-tokyo-deck/submit/` にも同じものを置いた。撮り方は [deck/README.md](../deck/README.md) に記録した

  **2026-08-23 に3点を実ファイルで再確認した**（この項目は前セッションの記述に依っていたため、変更不可の寸法規定を測り直した）。`01-plan` / `02-unanswered` / `03-gaps` はいずれも **実寸 1600×900**。3点とも目視し、**地図・第三者写真・いらすとや素材が写っていない**ことと、内容が上表と一致することを確認した（`01` は3停留地すべてに出典チップ＋CC BY 4.0 とカテゴリイラスト、`02` は `insufficient_granularity` と理由＋[Issue #192](https://github.com/fffokazaki/tabi-concierge-tokyo/issues/192) の記録される旨の注記、`03` は48件と理由別・エリア別の内訳＋「提出プロセス自体は未実装です」の構想バッジ）。**`03` の件数は撮影当時の48件**で、稼働中のカウンターは現在も増え続けている —— 提出するのはこの固定された3点であり、撮り直さない

  | # | 画面 | 見せているもの |
  | --- | --- | --- |
  | 1 | プラン | 3停留地すべてに出典チップ + CC BY 4.0、カテゴリイラスト、タブバー |
  | 2 | あなたへ（ラーメン） | `insufficient_granularity` と理由、記録される旨の注記 |
  | 3 | `/gaps` | 未回答の件数と理由別・エリア別の内訳 |

  `.app-shell` は 1600×900 のビューポートでも **480×874 固定**（内部がスクロールする端末モック）なので、ビューポートごと撮ればフレーム全体とタブバーが1枚に収まる。`/gaps` はデスクトップ幅のページなので画面いっぱいに写る。**`/gaps` の件数は稼働中のカウンターで増える** —— 撮影中にも 46 → 48 と動いた（アプリを操作した分が記録された）
- [x] **タブバーの見え方を確認** — スキャン・周辺は `AppTabs.tsx` の `INACTIVE_TABS` で disabled 表示。キャプチャだけを見る審査員が実装済みと誤認しないよう、スライド5で5画面それぞれに層バッジを付け、スキャン・周辺を「デザイン構想」と明示した（2026-08-22）。この2画面のキャプチャは `public/showcase/Japanese version.dc.html` のモックであり、実データが流れる画面ではない
- [x] **カタログURL の生存確認** — 確定10件を **2026-08-23 に再確認済み**（[DATABASE.md](02-design/DATABASE.md) §3 の指定どおり CKAN `package_show` で判定。カタログは WAF でブラウザ以外を弾くため HTTP ステータスでは判定できない）。10件すべて `success: true` / `state=active` / `private=false` / リソース1件以上、`license_id` は**全件 `CC-BY-4.0`**（スライド12の「全件 CC BY 4.0」の実物確認）。`metadata_modified` は §3 の記録値（2025-12-12・2026-03-30）から**変化なし** —— 「随時」更新の No.7・No.8・No.10 も差分なし
- [x] **必須4項目の充足** — ①課題及び解決策=スライド2・3・4（＋13 インパクト）／②プロダクト=5〜11／③利用オープンデータ=12／④チーム紹介=14。**v1.20.0 で上部ラベルの丸数字（①〜④）を外したため、資料上に「①②③④」の文字は無い**（対応は上記の割り当てで満たす）。丸数字を戻すかはチーム判断
- [x] **スライド11 の OSS 記述の確認** — OSS 化の対象は**コンシェルジュ（`worker/` `shared/` `scripts/` `migrations/`）のみ**で、旅行アプリ `src/` は含まない。切り出しの実施は提出後〜Final Stage（[Issue #129](https://github.com/fffokazaki/tabi-concierge-tokyo/issues/129)）のため、提出資料には「公開予定」と書く。**実物で確認**（2026-08-23）—— 「コンシェルジュ（バックエンド）を独立したリポジトリへ切り出し、MITライセンスで公開予定です。／旅行アプリのフロントエンドは対象外です。」＋`設計済み・未実装` バッジ、下段で「コード側｜MIT ライセンス（公開予定）」と「データ側｜CC BY 4.0（出典表示義務あり）」を別カードに並記
- [x] **著作権ルール（主催者規定4項目）を実ファイルで確認**（2026-08-23・生成した PPTX を unzip して判定）。詳細は [Issue #127](https://github.com/fffokazaki/tabi-concierge-tokyo/issues/127) のコメントに残した
  - **BGM なし** — `ppt/media/` は **PNG 13点のみ**。全14スライドと `presentation.xml` / `slideMaster1.xml` に `audioFile` / `videoFile` / `p:media` の参照が**0件**。`[Content_Types].xml` には `mp4` / `m4v` の Default 宣言があるが、これは pptxgenjs が常に出す定型で**対応するパートは存在しない**（unzip して grep した人が同じ疑問で止まらないよう明記する）
  - **『いらすとや』は1資料20個まで** — 使用**0個**（上限に対する余裕ではなく、素材そのものを使っていない）。デッキの画像は重複を除いて**10点**で、内訳は自作アプリ／自作モックの画面キャプチャ7点（`deck/img/deck-*.png`）＋メンバー本人提供画像から作成した円形ポートレート3点（`team-*.png`。発表者ノートの `[Sources]` に記録済み）。プラン画面に写るカテゴリイラストは**いらすとやではなく imagegen 生成物**（[CATEGORY_ILLUSTRATIONS.md](02-design/CATEGORY_ILLUSTRATIONS.md) §5 の記載。ここは文書記載であって画像の実測ではない）
  - **地図の帰属表示** — **地図を1枚も使っていない**ため規定の対象外。10点すべてを目視し、地図・航空写真・地図タイルが写っていないことを確認した。`src/` に地図ライブラリの依存は無く（`leaflet` / `mapbox` / `maps.google` いずれも0件）、showcase モックの `google` 参照は **Google Fonts**、`map` 参照は **`Array.prototype.map`**
  - **引用は出典明記** — スライド12が10件すべてに提供元・データセットID・カタログURL・CC BY 4.0 を明示。スライド6・7のキャプチャには出典チップ（データセット名・提供元・取得日・CC BY 4.0）が写り、スライド7には**マナー情報の「参考: JNTO」バッジと「日本政府観光局（JNTO）の公式ページ」リンクが実際に写っている**（オープンデータの「出典」と外部情報の「参考」を見た目で分ける [ADR-012](06-reference/DECISIONS.md) の実装が、資料の中でも確認できる）
- [x] **スライド5のスキャン画面キャプチャを撮り直した**（2026-08-23）。旧 `deck-scan-showcase.png` は画面見出し「カメラを向けて、解説を見よう。」が**ステータスバー帯とダイナミックアイランドに重なって潰れていた**。撮り方の誤りを疑って同条件で撮り直したが**同じ結果**になり、原因は showcase モック側だと確定した —— ステータスバーは `z-index: 10` の絶対配置オーバーレイ（フレーム上端から64px）で、スキャン画面だけがその下に本文の上余白を持たない（周辺画面は持つ）。**撮影時に本文へ64pxの上余白を与えて**他画面と同じ見え方で撮り直した。モック本体の修正は [Issue #221](https://github.com/fffokazaki/tabi-concierge-tokyo/issues/221)
- [ ] **提出物の外側に残っている著作権リスク1件** — `public/showcase/.image-slots.state.json` に**出典が記録されていない写真4点**（うち1点は実在店舗の外観写真）が data URI で埋め込まれ、**本番で誰でもダウンロードできる**（[Issue #222](https://github.com/fffokazaki/tabi-concierge-tokyo/issues/222)）。**PPTX には入っていない**（デッキが showcase から使うのはスキャン・周辺の2画面で、写真スロットがあるのはプラン・あなたへ側。デッキ内10点を全点目視して確認済み）が、スライド14に本番URLを載せているため審査員が到達しうる。素性が判別できないのはリポジトリ側の記録不足なので、**まず撮影者の確認**が必要
- [ ] **提出フォームの「利用オープンデータ」入力時の注意** — 登録は**データURL＋タイトル**。No.10 の資料表記「都市公園・都立公園一覧（渋谷区）」のうち **（渋谷区）は資料内の識別用補足**で、カタログの登録タイトルは「都市公園・都立公園一覧」。この名前は**37件のデータセットが共有している**（2026-08-23 実測。自治体ごとに同名）ため資料側の補足は外さず、フォームには `t131130d2025000003` のURLを添えて登録する
- [ ] **提出後ただちに収録枠を予約**（30分単位・早い者順）

## 参照

- [first-stage-presentation.md](first-stage-presentation.md) — 本書から切り出す2分版8枚
- [proposal-summary.md](proposal-summary.md) — 企画概要 v1.0
- [DATABASE.md](02-design/DATABASE.md) §2 — 利用オープンデータ確定10件
- [CONSTRAINTS.md](01-context/CONSTRAINTS.md) — 提出規定・著作権ルール
- [TASKS.md](07-project-management/TASKS.md) — 提出物タスクの進捗

## Changelog

### [1.21.0] - 2026-08-23

#### 変更

- **§6 提出前チェックリストを実ファイル照合の結果で埋めた**（[Issue #127](https://github.com/fffokazaki/tabi-concierge-tokyo/issues/127)）。生成した PPTX を unzip して主催者規定4項目（BGM／いらすとや／地図の帰属／引用の出典）を判定し、カタログURL10件を `package_show` で再確認、必須4項目とスライド11の OSS 記述を実物で確認した。**判定の根拠が「実測」か「文書記載」かを項目ごとに書き分けた**
- **§3・§4 の「キッカー」記述を v1.20.0 のラベル規則へ同期した。** 丸数字を外したのは v1.20.0 だが、§3 は「プロダクト ②　答えられないものを、オープンデータの要望へ」、§4 は「仕組み ②　AI × OPEN DATA」という**旧形式のまま据え置かれていた**（固定文言の節が実物と食い違っていた）。あわせて、ラベルから消えた「オープンデータの要望へ」の訴求を**本文最下段の1行が担っている**ことを明記し、冗長と誤解して消されないようにした
- **スライド5のスキャン画面キャプチャを撮り直した。** 見出しがステータスバー帯に潰れていた原因は撮影ではなく showcase モック側（スキャン画面だけ上余白が無い）と実測で確定させ、撮影時に上余白を与えて回避した。モック本体の修正は [Issue #221](https://github.com/fffokazaki/tabi-concierge-tokyo/issues/221)

#### 追加

- 提出フォームの「利用オープンデータ」入力時の注意を追加。No.10 のカタログ登録タイトル「都市公園・都立公園一覧」は**37件のデータセットが共有している**（自治体ごとに同名・2026-08-23 実測）ため、資料側の「（渋谷区）」表記は識別用として外さない
- 提出物の外側に残る著作権リスクを1件記録（[Issue #222](https://github.com/fffokazaki/tabi-concierge-tokyo/issues/222)）。`public/showcase/.image-slots.state.json` の出典未記録の写真4点が本番で取得可能。**PPTX には含まれない**

#### 検証

- **提出フォーム添付の3点（`deck/submit/`）を実ファイルで再確認した。** この項目は前セッションの記述に依っており、**変更不可の寸法規定（1600×900px）を一度も測っていなかった**。3点とも実寸 1600×900 で、地図・第三者写真・いらすとや素材が写っていないことを目視で確認した。著作権ルールの判定範囲は **PPTX ＋ この3点**

### [1.20.0] - 2026-08-22

#### 変更

- 全14枚の上部ラベルから、意味が伝わりにくかった応募要件の丸数字（①〜④）を削除した。`07 / 14　プロダクト｜訪日外国人向け`のように、**通し番号／総枚数＋ページの役割**で統一し、現在位置を一目で把握できるようにした

### [1.19.0] - 2026-08-22

#### 変更

- スライド7の主役を興味別レコメンドから、**訪日外国人が迷いやすい日本のマナーを旅先で確認できる価値**へ変更した。観光情報のオープンデータ「出典」と、マナーの JNTO 等による「参考」を分ける説明は維持した

### [1.18.3] - 2026-08-22

#### 変更

- 全14枚を実寸で確認し、スライド3・4・8・10・13・14の不自然な文字折り返しを修正した。単語途中で割れていた `CSV`・「循環」・カード内の固有表現を行単位で整え、スライド13の注記は意味を変えず簡潔にした

### [1.18.2] - 2026-08-22

#### 修正

- スライド11の左右カード本文を短くし、意味のまとまりごとに3行で改行を固定した
- 右カードの「MIT ライセンス」が「MI／T」に分割されないよう「MITライセンス」として同一行へ収めた

### [1.18.1] - 2026-08-22

#### 変更

- スライド14のsho gamohのイニシャルを、本人提供の写真からimagegenで作成した円形イラスト調ポートレートへ変更した
- 3名のポートレートを同じサイズ・同じ金色の縁取りで統一した

### [1.18.0] - 2026-08-22

#### 変更

- スライド14のshiwata・フトシのイニシャルを、本人提供のイラストを使った円形ポートレートへ変更した。shiwataのイラストは imagegen で顔〜肩が中心のアバターへ加工した
- shiwataのプロフィールに「証券会社勤務のアラフィフ」を追加した
- sho gamohのプロフィールに「18-year-old creator」を追加した

### [1.17.0] - 2026-08-22

#### 変更

- スライド9「つくるAI」の工程を「人＋AIで仕様を考える → docs/SSOT → AIが実装・検証 → 人が動作確認」へ変更した
- 人はAIと仕様を考えて意図を決め、AIが実装と機械検証を進め、最後に人が実際の動きを確認する役割分担を明示した

### [1.16.0] - 2026-08-22

#### 変更

- スライド8のキッカーを「答えられないものを、オープンデータの要望へ」へ変更し、未回答を不足データの改善につなげる構想を明示した
- 未回答の記録・分類は「稼働中」、東京都への公開リクエスト案への変換は「設計済み・未実装」とバッジと本文で区別した

### [1.15.1] - 2026-08-22

#### 変更

- スライド8の内部向け表現「この資料の山」を「答えられないものを記録」へ変更し、未回答を記録してデータ改善へつなぐサービス機能として示した

### [1.15.0] - 2026-08-22

#### 変更

- スライド5を実装状況の報告ではなくサービス全体像として再構成し、タイトルを「旅の準備から現地まで、AIエージェントが伴走。」へ変更した
- 稼働中3画面とデザイン構想2画面の区別は、タイトルではなく各画面の層バッジと脚注で示す構成にした

### [1.14.0] - 2026-08-22

#### 変更

- 表紙の「旅コンシェルジュTOKYO」直下に「Powered by オープンデータ・コンシェルジュ」を追加し、旅行アプリと基盤の2層構造を最初から示す構成にした

### [1.13.0] - 2026-08-22

#### 変更

- スライド10の主語を「2つのAIエージェント」へ変更し、旅コンシェルジュが旅行を支援し、オープンデータ・コンシェルジュが未回答を不足データの改善へつなぐ構造を明示した
- 稼働中の `gaps` 記録・分類と、設計済み・未実装の公開リクエスト案自動生成・提出を分けて表示し、「すべてAIで動かす」は目指す姿として表現した
- スライド10の4段階を「旅を支援する → 不足を記録する → 分類して束ねる → 要望案を自動生成」に更新した

### [1.12.0] - 2026-08-22

#### 変更

- スライド5を説明カード5枚から、5画面それぞれのスクリーンショットを並べる構成へ変更した
- 旅のプロフィール・プラン・あなたへには「稼働中」、`public/showcase/Japanese version.dc.html` から切り出したスキャン・周辺には「デザイン構想」の層バッジを付け、showcase のモックデータを実装済みと誤認しないようにした
- スライド5のキャプションで、実データ接続の3画面と showcase のモックデータ2画面を明示し、提出前チェックリストのタブバー確認を完了へ更新した

### [1.11.0] - 2026-08-22

#### 変更

- スライド9を「AIでつくり、AIで届ける。」の上下2レーンへ再構成。上段に `docs/` を SSOT とした Claude Code / Codex による AI仕様駆動開発、下段に訪日外国人の自然文 → メタデータRAG → Text-to-SQL / D1実照会 → 出典つき旅程・マナーを集約した
- 一次ユーザーを表紙・課題・仕組みで「訪日外国人」と明示。自然文をLLMで構造化する既存経路を活かした**多言語対応予定**を、稼働中の機能とは区別して追加した
- マナー情報は JNTO 等の「参考」であり、東京都オープンデータの「出典」と区別することをスライド9にも明記した
- スライド14から重複する開発スタイル帯を外し、チームメンバーと役割に集中させた

### [1.10.0] - 2026-08-22

#### 修正

- 「必須4項目」の「須」が別字（U+5FC5 U+9885）に置換されていた3箇所を修正（[Issue #211](https://github.com/fffokazaki/tabi-concierge-tokyo/issues/211)）。v1.9.0 の編集で入った置換ミス。提出要件の固定語であり、提出前チェックリストの項目名でもあった。**壊れた字そのものは本書に書かない** —— Issue #211 の受け入れ条件が「検索して0件」であるため、引用が自分でゲートに引っかかる

#### 変更

- **v1.9.0 で未実施だった `deck/build.js` への同期を実施**（[Issue #211](https://github.com/fffokazaki/tabi-concierge-tokyo/issues/211)）:
  - スライド9を「React／コア3操作／D1」の3層から、**自然文 → メタデータRAG → Text-to-SQL → 出典強制の4段パイプライン**へ。層と `/mcp` 公開は1枚のカードへ集約（[Issue #117](https://github.com/fffokazaki/tabi-concierge-tokyo/issues/117) / [#119](https://github.com/fffokazaki/tabi-concierge-tokyo/issues/119) / [#120](https://github.com/fffokazaki/tabi-concierge-tokyo/issues/120)）
  - スライド14に開発スタイル（AI 仕様駆動開発）の帯を追加
- **キャプチャ3枚を撮り直した**（§5 参照）。スライド6/7は画面上部のみの表示に変え、キャプションをタブバーに言及しない `SCROLL_CAPTION` へ分離した

#### 修正（セルフレビュー指摘）

- スライド9の「メタデータRAG」の説明が「約9,600件のカタログからデータセットを特定」になっていたのを「収録10件のメタデータから候補を特定」へ修正。実装は**確定10件のメタデータをプロンプトへ全件渡す方式**（Vectorize 不採用・`worker/core/interpret.ts`）で、9,600件は「カタログにある数」であって「検索している数」ではない。「稼働中」バッジ付きで未実装範囲を稼働中として提示していた（絶対ルール #1）
- スライド9の4段パイプラインが「常に直列に通る」と読める構成だったのを修正。**興味チップの呼び出しは構造化入力なので2段目（メタデータRAG）を飛ばす**（`operations.ts` の `interpreted()`）。1段目の説明が「自然文」と「プロフィールと興味」を混ぜていたのを自然文だけに絞り、スライド上に経路の断り書きを1行追加した。**§2 のスライド9行（SSOT 側）も同時に限定した** —— ここを直さないと次の同期で `build.js` へ戻ってくる
- §5 の数値を再導出可能にした。幅の範囲が自分の上限（2.25in）を含んでいなかったのを 1.6〜2.3in へ、pt の換算基準（アプリ本文 11.5px・本番実測）を明記、「480×874→1550」の矢印記法が「旧キャプチャ時点のフレーム高」と「いまのフレーム高」を1つに潰していたのを分けて書いた
- §5 のキャプチャ項目が**2つの別成果物を混同していた**のを分離。資料に埋め込む端末フレーム画像（480×874）と、提出フォームに添付する画面キャプチャ（1600×900px・規定）は別物で、後者は**未実施**として明示した。混同は本 PR 以前からあり（旧文は「スライド6のキャプチャを撮り直す（1600×900px）」と両者を1項目に混ぜていた）、本 PR の書き換えで 1600×900 の要件追跡が一度消えていた

### [1.9.0] - 2026-08-22

#### 変更

- スライド9に技術的特異点のパイプライン（自然文 → メタデータRAGでデータセット特定 → Text-to-SQLでD1実照会 → 出典強制、同じコア3操作を `/mcp` でも公開）の明記を追加。いずれも稼働中の実装のみを記載（[Issue #117](https://github.com/fffokazaki/tabi-concierge-tokyo/issues/117) / [#119](https://github.com/fffokazaki/tabi-concierge-tokyo/issues/119) / [#120](https://github.com/fffokazaki/tabi-concierge-tokyo/issues/120)）
- スライド14（§4）に開発スタイルを追加。仕様書（`docs/`）を SSOT とした Claude Code / Codex による AI 仕様駆動開発（フルオート実装）をチーム紹介でアピールする
- **`deck/build.js` への同期は未実施**。スライド9・14の文言変更を `build.js` 側にも反映すること

### [1.8.0] - 2026-08-22

#### 追加

- 冒頭に PPTX ビルダー [`deck/`](../deck/) への参照を追加（[PR #202](https://github.com/fffokazaki/tabi-concierge-tokyo/pull/202)）。ビルダーはセッション固有の一時領域にしか無く、原稿から辿れなかった。併せて `deck/build.js` 側を本書へ同期した:
  - スライド13の「30分→3分」を v1.6.0 の確定文言（体験・操作一巡に基づく目安）へ差し替え。v1.3.0 当時の「企画時の推定値・未実測」が残っていた
  - スライド12にデータセットIDの列を追加し、カタログURL をテンプレートから解決可能な形へ。件数の説明を実データに合わせて修正（列の合計 1,667 と注記の「合計 1,645」が 22 件ズレていた。差は No.9 の統計表で、`spots` へは取り込んでいない）
  - スライド14の URL を移設前の `opendata-002` から現行の `tokyo-odh-091` へ差し替え（旧アカウントのデプロイは提出までのロールバック先・[DEPLOYMENT.md](05-operations/DEPLOYMENT.md)）
- **`deck/img/deck-plan.png` は Issue #188 の反映後の画面ではない。** 直上の 1.7.0 が定めたスライド6の撮り直しは、本書のチェックリストだけでなく `deck/img/deck-plan.png` の差し替えも意味する（`build.js` はこのファイル名を固定参照しているため、同じ名前で上書きすれば足りる）

### [1.7.0] - 2026-08-22

#### 変更

- Issue #188 でスライド6のプラン画面にカテゴリイラストが加わるため、旧キャプチャを不一致として撮り直し必須へ変更。対象外のスライド7/8は取得済みのまま維持

### [1.6.0] - 2026-08-22

#### 変更

- §1「使う数字は3つに絞る」とスライド13の「30分→3分」の扱いを確定（[Issue #130](https://github.com/fffokazaki/tabi-concierge-tokyo/issues/130)）。「企画時の推定値」から「30分＝開発メンバーの初回探索体験に基づく目安・3分＝操作一巡の目安（生成応答は本番で押下から約13秒後に表示済みを確認）」へ根拠を差し替え。「実測」と表記しない方針は維持

### [1.5.0] - 2026-08-22

#### 変更

- §5 キャプチャ行の判断を確定（[Issue #172](https://github.com/fffokazaki/tabi-concierge-tokyo/issues/172)）。撮り直し不要 — 新本番で3画面とも同一操作を再現し、表示内容の一致を実測した。一致の前提が [Issue #174](https://github.com/fffokazaki/tabi-concierge-tokyo/issues/174) の行選定修正であることも明記

### [1.4.0] - 2026-08-22

#### 変更

- §5 のキャプチャ行を事実に合わせて訂正。**取得元は移設前の旧URL（Version `712c2277`）**であることを明記し、画像自体の差し替えが不要な根拠（アドレスバー非写り込み・取得後の変更はいずれも正常系の表示を変えない）を残した。撮り直しの要否は [Issue #172](https://github.com/fffokazaki/tabi-concierge-tokyo/issues/172) へ切り出した

### [1.3.0] - 2026-08-22

#### 変更

- §1 の3層表を実装の現況に合わせた。**MCP サーバー公開と Text-to-SQL を「設計済み・未実装」から「稼働中」へ移した** — `/mcp` は本番で `tools/list` が3ツールを返し（2026-08-22 実測）、`aggregate_dataset` は D1 実照会で応答する（同日実測。[Issue #117](https://github.com/fffokazaki/tabi-concierge-tokyo/issues/117) / [#119](https://github.com/fffokazaki/tabi-concierge-tokyo/issues/119) / [#120](https://github.com/fffokazaki/tabi-concierge-tokyo/issues/120) はいずれもマージ・デプロイ済み）。本書 v1.0 は #117 のマージ当日に書かれており、その時点の状態のまま据え置かれていた
- 「設計済み・未実装」には、データ公開リクエストの都への提出と、コンシェルジュの別リポジトリ切り出し・MIT 公開（[Issue #129](https://github.com/fffokazaki/tabi-concierge-tokyo/issues/129)）が残る

### [1.2.0] - 2026-08-22

#### 変更

- §5 チェックリストの「キャプチャ3点」を取得済みに更新（[Issue #126](https://github.com/fffokazaki/tabi-concierge-tokyo/issues/126)）。**撮るときの操作条件**（興味チップのみ・自由文は空／スライド6はラーメンを外す）を根拠つきで明記した。ラーメンを入れたままだとスライド6に未回答バナーが出て、§2 が定めた「出典を見せてから答えられないへ落とす」順序が崩れる
- §5「タブバーの見え方を確認」に、確認の結果（薄いグレーのラベルで壊れて見えはしないが、キャプチャ単体では未完成と読める）と、キャプション側に明示する方針を追記した。記載自体は [Issue #127](https://github.com/fffokazaki/tabi-concierge-tokyo/issues/127) で行うため未チェックのまま残している

### [1.1.0] - 2026-08-21

#### 変更

- スライド11 の OSS 記述を具体化。「ソースコードを MIT で公開予定」から「**コンシェルジュを別リポジトリへ切り出して** MIT で公開予定」へ改め、OSS 化の対象がコンシェルジュのみ（旅行アプリ `src/` は対象外）であることを明記した（[Issue #129](https://github.com/fffokazaki/tabi-concierge-tokyo/issues/129)）
- §5 チェックリストの「`LICENSE` の設置」を、切り出し時期（提出後〜Final Stage）に合わせて「スライド11 の OSS 記述の確認」へ置き換えた

### [1.0.0] - 2026-08-21

#### 追加

- 初版作成。提出資料（16:9・必須4項目）の14枚構成、3層区別ルール、スライド8の固定文言、チーム紹介、提出前チェックリストを定義した
