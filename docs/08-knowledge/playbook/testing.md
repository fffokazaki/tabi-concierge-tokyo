# PLAYBOOK — テスト・検証 (testing)

> **Parent**: [PLAYBOOK.md](../PLAYBOOK.md) — 運用ルール・エントリテンプレート・ID規則・記述ガイドラインは親ファイルの SSOT を参照。
>
> 新規エントリは本ファイル末尾に追記し、[PLAYBOOK.md の索引テーブル](../PLAYBOOK.md#エントリ一覧)にも 1 行追加する。

---

## エントリ一覧

<a id="ace-9-2"></a>

### ACE-9-2: ランタイムの同一性は、実行中のランタイム自身に答えさせて確認する

| Category | testing | Origin | PR #9 |
| Date | 2026-08-15 |
| Helpful | 0 | Harmful | 0 |
| Status | active |

「ローカルも本番と同じランタイムで動いているか」は設定ファイルの読み合わせでは確認できない。ツールが黙って別ランタイムへフォールバックしても設定は正しいままだからである。ヘルスチェックに `navigator.userAgent` のようなランタイム自己申告値を含めておくと、ローカルと本番の両方で1回 curl するだけで実測できる。環境差が疑われる問題（本番だけ壊れる系）の切り分け時間も短縮される。

---

<a id="ace-23-2"></a>

### ACE-23-2: ステータスコードを根拠にする前に、対照群でそのコードの意味を確かめる

| Category | testing | Origin | PR #23 |
| Date | 2026-08-16 |
| Helpful | 0 | Harmful | 0 |
| Status | active |

確定した10件のカタログURLが実在するかを `curl` で検証したところ、**全件 403** が返った。URL が壊れているように見えるが、実際は WAF がブラウザ以外のアクセスを遮断していただけだった。User-Agent を偽装すると 202 になるが、**存在しない ID でも同じ 202** が返るため、どちらのコードも実在判定に使えない。対照群（存在しないIDを1件混ぜる）を取らずに最初の 403 を報告していれば「URL が全部死んでいる」という誤報になり、逆に UA 偽装後の 202 を成功と読めば「全件実在」という誤検証になっていた。ブラウザで開くと実在ページは表示され、存在しない ID は 404 ページになり、初めて判定できた。**外形監視・URL 実在確認・疎通確認では、成功と失敗の両方の入力を通してコードが実際に区別されることを確かめてから、そのコードを根拠に使う。** 判定に使えない環境だと分かった場合は、その事実と代替の確認方法を文書に残す（[DATABASE.md](../../02-design/DATABASE.md) §3 に記録済み）。

---
