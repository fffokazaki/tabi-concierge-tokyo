---
title: "LLM-MODEL-CANDIDATES"
version: "1.1.0"
status: "active"
owner: "@fffokazaki"
created: "2026-08-21"
updated: "2026-08-21"
changeImpact: "low"
---

# LLM-MODEL-CANDIDATES.md - Workers AI モデル候補比較

> **決定（2026-08-21・Futoshi）**: メタデータRAG / Text-to-SQL に使うモデルは **`@cf/qwen/qwen3-30b-a3b-fp8`** とする。
> ADR-002 が記載していた `@cf/meta/llama-3.1-8b-instruct-fp8-fast` からの変更。理由は §2。
> ARCHITECTURE.md §8・MASTER.md の技術スタック表への反映と ADR への追記は Issue [#118](https://github.com/fffokazaki/tabi-concierge-tokyo/issues/118) の実装時に行う（親 Issue: [#121](https://github.com/fffokazaki/tabi-concierge-tokyo/issues/121)）。

## 1. 背景

Step 3〜5（Issue #121）で LLM に担わせる仕事は「訪日観光客の質問文（日本語・英語ほか多言語）を構造化入力へ分解し、カタログ10件からデータセットを選定し、単一テーブルへの SELECT を生成する」こと。当初案の Llama 3.1 は**公式サポート8言語（en/de/fr/it/pt/hi/es/th）に日本語が入っておらず**、日本語理解が選定基準として不足していた。

## 2. 候補比較（価格は 2026-08-21 に公式ドキュメントで実測）

出典: <https://developers.cloudflare.com/workers-ai/platform/pricing/>

コスト列の前提: search 呼び出し ≈ 入力1,800/出力150トークン、aggregate 呼び出し ≈ 入力900/出力120トークン、旅程1本 = search 1回 + aggregate 4回。無料枠は 10,000 ニューロン/日。

| モデル | 日本語力 | 入力（neurons/M tok） | 出力（neurons/M tok） | ニューロン/プラン | 無料枠での目安 | 寸評 |
|---|---|---|---|---|---|---|
| `llama-3.1-8b-instruct-fp8-fast`（当初案） | △ 非公式 | 4,119 | 34,868 | ~45 | ~220プラン/日 | 基準。日本語は公式サポート外 |
| **`qwen3-30b-a3b-fp8`** ✅採用 | **◎** | 4,625 | 30,475 | **~45** | **~220プラン/日** | Qwen は CJK 学習が厚く日本語最強格。MoE（アクティブ3B）のため価格が 8B 並み。**日本語力を上げてコスト据え置き** |
| `gpt-oss-20b` | ○ | 18,182 | 27,273 | ~117 | ~85プラン/日 | 指示追従・JSON 出力が堅実。多言語は英語寄り |
| `gpt-oss-120b` | ○+ | 31,818 | 68,182 | ~216 | ~46プラン/日 | 候補中の総合力最強。品質で困ったときの上位互換 |
| `llama-3.3-70b-instruct-fp8-fast` | △ 非公式 | 26,668 | 204,805 | ~275 | ~36プラン/日 | 賢いが日本語は依然非公式・出力単価が高い |
| `gemma-3-12b-it` | ○（140言語） | 31,371 | 50,560 | ~150 | ~66プラン/日 | 多言語は広いが qwen3 への優位なし |
| `qwq-32b` | ◎ | 60,000 | 90,909 | ~330+ | — | 長考型の推論特化。レイテンシ・単価で不利。見送り |
| `qwen2.5-coder-32b-instruct` | ◎ | 60,000 | 90,909 | ~330+ | — | コード特化。SQL 生成のみへの用途別起用なら将来候補 |

## 3. 採用理由と留意点

**採用理由**:

1. 日本語（および中国語・韓国語などアジア圏言語）の理解が候補中で最も厚く、訪日観光客の質問分解という用途に合致する
2. ニューロン単価が当初案とほぼ同額（入力 4,625 vs 4,119 /M tok）。MoE 構造（総パラメータ30B・アクティブ3B）のため「賢くする＝高くなる」のトレードオフが成立しない稀な選択肢
3. 品質不足時の上位互換として `gpt-oss-120b`（約5倍・~46プラン/日）を控えに置ける

**留意点**:

- **`qwen3-30b-a3b-fp8` は reasoning（思考）モデルである。** 素で呼ぶと本文の前に思考トークンを大量に吐く。§4 に実測と対処を記載した（**実装前に必ず読むこと**）
- モデル名は `worker/core/llm.ts` の定数1箇所。`LlmRequest.purpose` により将来「分解は qwen3・SQL 生成は coder 系」の用途別振り分けも interface 変更なしで可能

## 4. 実測挙動（2026-08-21・`npm run dev` のローカル実 API・AI Gateway 経由）

§2 の表は**公表価格からの試算**だった。ここは実際に `env.AI.run()` を叩いて測った結果を記録する。

### 4.1 思考（reasoning）を止めないと本文が出ない

公式のモデルページは `@cf/qwen/qwen3-30b-a3b-fp8` に **Reasoning: Yes** と明記している。実際、素で呼ぶと `message.content` の前に `message.reasoning_content` へ思考が出る。同一の質問（「浅草でトイレを探しています。JSON で `{"areas":[...],"interests":[...]}` だけ返してください。」）を `max_tokens: 512` で条件だけ変えて叩いた結果:

| 条件 | `finish_reason` | ニューロン | 出力トークン | 思考の長さ | 本文 |
|---|---|---|---|---|---|
| 素（思考あり） | `stop` | 11.70 | 379 | 1,308字 | 完全な JSON |
| **`/no_think` を末尾に付ける** | `stop` | **1.79** | **53** | 2字 | **完全な JSON** |
| `enable_thinking: false` を渡す | **`length`** | 15.76 | 512（上限到達） | 2,050字 | **途中で切断** |
| `response_format: { type: "json_schema", ... }` | **`length`** | 15.76 | 512（上限到達） | 1,915字 | **途中で切断** |

読み取れること:

1. **効くのは `/no_think` だけ。** `enable_thinking: false` は Cloudflare のエンドポイントに無視される（Qwen 本家のパラメータであってこの経路の入力仕様ではない）。`response_format` も思考を止めない
2. **止めないと壊れ方が悪い。** 思考が `max_tokens` を食い尽くし、`finish_reason: "length"` で **JSON が途中で切れる**。これはパーサをいくら寛容にしても復元できない（閉じ括弧が存在しない）。「壊れた出力に寛容なパーサ」で吸収できる問題ではなく、**入力側で止めるしかない**
3. **コストが 6.5 倍・レイテンシが 3.5 倍違う**（11.70 → 1.79 ニューロン、5,560ms → 1,605ms）

### 4.2 ニューロン単価の検算

3件の実測（`(入力, 出力) → ニューロン` = `(31,64)→2.094`、`(37,53)→1.79`、`(33,379)→11.70`）から連立で解くと:

| | 実測から算出 | §2 の公表価格 | 一致 |
|---|---|---|---|
| 入力 | 約 4,750 neurons/M tok | 4,625 | ✅ |
| 出力 | 約 30,500 neurons/M tok | 30,475 | ✅ |

**§2 の試算表は正しい。** ただし前提の「出力120〜150トークン」は**思考を止めた場合の値**である。

| 前提 | ニューロン/プラン | 無料枠（10,000/日）での目安 |
|---|---|---|
| `/no_think` あり（§2 の前提どおり） | ~45 | **~220プラン/日** |
| `/no_think` なし（思考が乗る） | ~99 | ~100プラン/日 ＋ **切断のリスク** |

> 出力トークンの単価は入力の **6.4倍**。予算の主戦場は出力を削ることであり、プロンプト（入力）を厚くしてでも出力を短く確定させる方が安い。カタログ10件をプロンプトに全件同梱する設計（[Issue #120](https://github.com/fffokazaki/tabi-concierge-tokyo/issues/120)）はこの意味でも整合する。

### 4.3 実装への申し送り

- **必須**: `worker/core/llm.ts` はプロンプト末尾に `/no_think` を付ける。付け忘れをテストで固定する（付いていることの検査）
- **必須**: それでも `message.content` が `null` / 空になりうる前提でパースする（思考モデルは `content: null` を返す形を持つ）。`finish_reason: "length"` は出力不正として扱い、リトライか縮退へ倒す
- **使わない**: `enable_thinking` / `response_format` を思考抑止の手段として当てにしない（実測で効かない）
- ここの数値は**ローカル実 API の1回計測**。プロンプトが実寸（カタログ同梱で入力1,800トークン級）になったときの再検算は [Issue #120](https://github.com/fffokazaki/tabi-concierge-tokyo/issues/120) の DoD に残っている

## Changelog

| 日付 | version | 変更 |
|---|---|---|
| 2026-08-21 | 1.1.0 | §4「実測挙動」を追加（[Issue #116](https://github.com/fffokazaki/tabi-concierge-tokyo/issues/116)）。`qwen3-30b-a3b-fp8` が reasoning モデルであること、思考を止められるのは `/no_think` だけで `enable_thinking` / `response_format` は効かないこと、止めないと JSON が切断されること、公表価格の検算結果を記録。§3 の留意点から解決済みの2項目を §4 へ移した |
| 2026-08-21 | 1.0.0 | 新規作成。Workers AI テキスト生成モデル8候補の日本語力・価格比較と `qwen3-30b-a3b-fp8` 採用の決定を記録 |
