# API.md - API設計書（MCP ツール仕様）

> 本システムの外部インターフェースは REST/GraphQL ではなく **MCP（Model Context Protocol）のツール**である。フロントエンド「旅コンシェルジュTOKYO」は MCP クライアントとして、バックエンド「オープンデータ・コンシェルジュ」が公開するツールを呼び出す。
>
> **ツール名・引数スキーマはすべて仮称であり未確定。** POC 実装時に確定させ、本書を更新すること（推測で確定扱いにしない）。

## 1. API概要

### API基本情報

| 項目 | 内容 |
| ---- | ---- |
| 種別 | MCP サーバー（ツール公開） |
| エンドポイント | 未定（Cloudflare 上のデプロイ先が決まり次第記載） |
| プロトコルバージョン | 未確認（実装時に確定） |
| 認証 | なし（POC。§2 参照） |
| 想定クライアント | 旅コンシェルジュTOKYO（第1クライアント）／翌年のハッカソン参加者が作る任意のクライアント |

### API設計原則

1. **最小3ツールから始める**: データセット検索・集計・出典取得。必要性が実証されたツールだけを追加する
2. **出典なしの応答を返さない**: 根拠となるデータセットを提示できない場合、ツールは「回答なし」を明示的に返す。曖昧な推測を返さない
3. **未回答は失敗ではなく出力**: 答えられなかった質問は `unanswered` として記録し、データ公開リクエストの入力にする（silent failure を作らない）
4. **アプリ固有の語彙を持ち込まない**: 「旅程」「マナー」等の旅行アプリ都合の概念はクライアント側の責務。ツールはデータセットの検索・集計・出典という汎用語彙に留める（基盤開放のため）

## 2. 認証・認可

- **認証方式**: なし（POC では利用者認証を伴う機能を実装しない）
- **認可モデル**: 該当なし
- **将来**: MCP サーバーを外部公開（翌年参加者への開放）する段階で、レート制限を含めて再設計する

## 3. ツール一覧

### 3.1 `search_datasets`（仮称）— データセット検索

自然言語の質問から、答えの根拠になりうるデータセットをメタデータRAG で特定する。

| 項目 | 内容 |
| ---- | ---- |
| 入力 | 質問文（自然言語）、任意でエリア・カテゴリ等の絞り込み条件 |
| 出力 | データセット候補のリスト（カタログID・タイトル・提供元・URL・適合理由） |
| 未確定 | 引数スキーマ、候補件数の上限、スコアリング方法 |

### 3.2 `aggregate_dataset`（仮称）— 集計

特定したデータセットに対して、その場で集計・抽出を行う（Text-to-SQL）。

| 項目 | 内容 |
| ---- | ---- |
| 入力 | データセットID、集計意図（自然言語または構造化条件） |
| 出力 | 集計結果と、**実行したクエリ**（出典に添えるため必須） |
| 未確定 | 引数スキーマ、対応する集計操作の範囲、エラー時の返却形式 |

### 3.3 `get_provenance`（仮称）— 出典取得

回答に添付する出典情報（データセットリンク・提供元・ライセンス表記・取得日）を構造的に返す。

| 項目 | 内容 |
| ---- | ---- |
| 入力 | データセットID（複数可）、実行クエリ（集計を経た場合のみ。検索のみの経路では検索条件） |
| 出力 | 出典チップ表示に必要な構造化データ（データセットID・タイトル・URL・提供元・CC BY 4.0 表記・取得日。§4 の必須項目と同一） |
| 未確定 | 引数スキーマ、複数データセット横断時の表記ルール |

### 3.4 追加候補（未実装）

| ツール | 用途 | 状態 |
| ------ | ---- | ---- |
| `recommend_spots`（仮称） | 興味ベースのスポット推薦（「あなたへ」画面） | 検討中。最小3ツールの組み合わせで代替できるか要検証 |
| `report_gap`（仮称） | 未回答をデータ公開リクエストとして登録する | 検討中（[DOMAIN.md](./DOMAIN.md) §7） |

### フロントエンド5機能とツールの対応

| 画面 | 呼び出すツール |
| ---- | -------------- |
| 👤 旅のプロフィール | （ツール呼び出しなし。パーソナライズ条件をコンテキストとして保持） |
| 🗺️ プラン | `search_datasets` → `aggregate_dataset` → `get_provenance` |
| 📷 スキャン | 画像認識＋施設・文化データ照会（Final Stage 構想。ツール未定義） |
| 📍 周辺 | `search_datasets` → `get_provenance`。欠損検知時は還元エンジンへ |
| ✨ あなたへ | `search_datasets` → `get_provenance`（推薦ロジックはクライアント側） |

## 4. 共通仕様

### 応答の必須要素

すべてのツール応答は次のいずれかの形をとる。

1. **回答あり**: 結果 ＋ 根拠情報
2. **回答なし**: 「該当するオープンデータがありません」＋ 未回答理由の分類（データ未公開 / 粒度不足 / 対象エリア外 等）

「根拠情報」の必須項目はツールの性質で決まる。

| ツール | 根拠情報として必須 |
| ------ | ------------------ |
| `search_datasets` | データセットID・タイトル・提供元・URL（**実行クエリは持たない**。検索条件を添える） |
| `aggregate_dataset` | 上記に加えて**実行クエリ**（集計を伴うため） |
| `get_provenance` | データセットID・タイトル・提供元・URL・ライセンス表記・取得日。集計を経た場合はその実行クエリ |

- 出典を伴わない「回答あり」は**仕様違反**として扱う
- 集計を経ない経路（「周辺」「あなたへ」のように `search_datasets` → `get_provenance` と辿る場合）では、実行クエリの代わりに検索条件を根拠情報とする。**実行クエリの不在を理由に出典を省略してはならない**

### エラーレスポンス

- 未定（MCP のエラー表現に合わせて実装時に定義する）
- 「データが見つからない」は**エラーではなく正常な回答なし**として区別すること

### ページネーション

- 未定（候補件数の上限で対応できるか実装時に判断）

## 5. レート制限

- 該当なし（POC。外部公開時に再検討）

## 6. WebSocket API

- 該当なし

## 7. GraphQL API

- 該当なし

## 8. バージョニング

- ツール名・引数スキーマの破壊的変更が発生した場合は、[DECISIONS.md](../06-reference/DECISIONS.md) に記録する
- 外部クライアント（翌年参加者）が現れる段階で、明示的なバージョニング方針を定める

## 9. セキュリティ

- **CORS設定**: 未定（デプロイ先確定後に決定）
- **セキュリティヘッダー**: 未定
- **入力の扱い**: Text-to-SQL の生成クエリが同梱データ以外へ到達しないことを実装時に確認する

## 10. 開発者向け情報

- **SDK**: MCP クライアント実装に準ずる（独自 SDK は提供しない）
- **APIドキュメント**: 本書がその役割を担う
- **サンドボックス環境**: 未定
